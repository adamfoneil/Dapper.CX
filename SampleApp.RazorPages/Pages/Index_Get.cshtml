<p>Here's what a typical use of <code>GetAsync</code> would look like in a Razor page. This snippet uses the <a href="https://github.com/adamfoneil/Dapper.CX/blob/master/SampleApp.Models/Item.cs">Item</a> model class.</p>

<p>The </p>

<pre><code class="csharp">
// identity property passed through route or query string
[BindProperty(SupportsGet = true)]
public int Id { get; set; }

// your model class type to interact with
public Item Item { get; set; }

public async Task OnGetAsync()
{
    Item = await Data.GetAsync<Item>(Id);
}
</code></pre>